pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            url = uri("https://global.repo.jmix.io/repository/public")
        }
    }
}

rootProject.name = "MeteoAPI"
